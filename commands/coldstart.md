---
name: coldstart
description: Open the session — read the pointer, open the work file it names, read the declared list, stop. Refuses to start work over a blocker.
---

Read `skills/coldstart/SKILL.md` and follow it in order. The whole walk lives there; this wrapper
is resident and the skill is not.

The resume reads the pointer's `reading` list as a ceiling and never loads a folder wholesale.
A non-empty `blockers` stops it before any work begins.
