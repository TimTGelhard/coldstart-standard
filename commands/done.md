---
name: done
description: Close the active session — verify first, file the outcome, regenerate the three indexes, rewrite the pointer whole, commit.
---

Read `skills/done/SKILL.md` and follow it in order. The whole close lives there; this wrapper is
resident and the skill is not.

The close refuses on a red verify. It is the single writer of the pointer and the single caller of
`tools/index.py`.
