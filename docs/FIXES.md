# Fixes — the open queue

<!-- One line per open item, pointing into fixes/. Open-only: a shipped item is deleted, because
     git is the archive. No done section, ever. Grammar: docs/FORMAT.md section 5.
     Generated by tools/index.py; edit the topic files, not this region.
     GENERATED BELOW THIS LINE -->

- **The harness has no measured floor** — the family contract makes MEASURE.md mandatory and this tree has none, so every byte claim is still an estimate · [fixes/audit.md](fixes/audit.md) · since 2026-08-21 · later
- **The safety floor in this tree is borrowed** — hooks/ is written but untracked and registered nowhere, so the floor that actually fires here is a neighbour's · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **install.sh evicts entries from the global Claude surface** — the installer moves ~/.claude commands and skills into backup directories, so a project script mutates the machine · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **The payload is installed by copy with nothing checking for drift** — commands/ and skills/ exist twice, at the root and under .claude/, and no check compares them · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **Every proof in this tree is written by the session that produced it** — the loop is evidenced by markdown artifacts authored by the run they describe, which is a self-report · [fixes/audit.md](fixes/audit.md) · since 2026-08-21 · later
- **The committed indexes disagree with their folders at HEAD** — index.py --check exits 1 on a clean checkout; the queue is missing all of section 4 and section 6 · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **Two sessions' worth of work sits outside the close protocol** — install.sh shipped with its session still pending and hooks/ is untracked, so the record understates the tree · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **The floor's secrets category outgrew the plan that describes it** — the floor now refuses to stage a dotenv file as well as read one, and section 4's plan does not mention it · [fixes/audit.md](fixes/audit.md) · since 2026-08-21
- **The ColdStart parking card still says parked** — the control tree records the program as parked, which the 2026-08-21 unparking contradicts · [fixes/family.md](fixes/family.md) · since 2026-08-21
- **coldstart-minimal SPEC has not been amended to this memory model** — confirmed stale: still stamped parked, still lists the warehouse under abandons, has no three-index model · [fixes/family.md](fixes/family.md) · since 2026-08-21 · later
- **CHARTER.md is stale on the unparking** — the charter still carries the 2026-08-19 parked banner and gates the downstream harnesses on a score · [fixes/family.md](fixes/family.md) · since 2026-08-21
- **The ColdStart control tree has not moved into claude-harnesses/** — the charter says the tree moves once the 7-day soak closes on 2026-08-22; until then a symlink stands · [fixes/family.md](fixes/family.md) · since 2026-08-21 · later
- **The v1 install claims any tree holding `docs/PROGRESS.md`** — v1's floor governed this repo against a pointer schema it cannot read, and the workaround leans on a foreign error path · [fixes/family.md](fixes/family.md) · since 2026-08-21
- **coldstart-coding's unblock condition cannot be met as written** — coding gates itself on standard having an eval score, and the comparator that would supply one was dropped unbuilt · [fixes/family.md](fixes/family.md) · since 2026-08-21
- **The close has no exemption from the mode floor it writes** — the safety floor reads the pointer's mode the moment it lands, so a close can deny its own commit · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21 · later
- **Testing the blocker path needs a pointer writer that is not `/done`** — the resume's refusal path can only be exercised by planting a blocker the close has no reason to write · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21 · later
- **The floor's escape hatch names a tool this harness does not have** — the mode-contract refusal tells the operator to run tools/done.py --set-mode build, and tools/ holds index.py alone · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21 · later
- **`/prep`'s output is not resumable until an unrelated session closes** — a freshly planned section is invisible to a cold resume: only `/done` repoints the pointer and regenerates the queue · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21
- **The three commands are registered nowhere, so they cannot be typed** — `commands/` and `skills/` sit unmapped at the repo root, so typing `/coldstart` here loads v1's skill instead · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21
- **The cold-agent check on the floor was not run** — every floor refusal on file was provoked by the author knowing the rule · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21 · later
- **Resolved paths were compared against unresolved roots, twice** — on macOS /var is a symlink to /private/var, and both checks read it as "outside the repo" · [fixes/lifecycle.md](fixes/lifecycle.md) · since 2026-08-21 · later
