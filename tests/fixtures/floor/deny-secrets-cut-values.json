{
  "note": "field 2 is the values; the key-only exemption must not over-reach",
  "mode": "build",
  "pointer": "standard",
  "expect": "deny",
  "category": "secrets",
  "payload": {
    "tool_name": "Bash",
    "tool_input": {
      "command": "cut -d= -f2 .env"
    }
  }
}
